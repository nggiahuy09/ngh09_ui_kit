# The Widgetbook catalog reuses the package's strict lint set.
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Catalog entry points are fine without library-level docs.
    public_member_api_docs: false
