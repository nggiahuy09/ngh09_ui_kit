/// The corner-radius shape of a `GHAppBadge`.
///
/// Matches the two corner styles in the Finesse UI Kit "Badges" component set.
enum BadgeCorner {
  /// Squared-off corners (the kit's `radii.sm` scale, 4 dp).
  sharp,

  /// Fully rounded, pill-shaped corners.
  smooth,
}
