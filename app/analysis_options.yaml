# The example app reuses the package's strict lint set so demos stay idiomatic.
analyzer:
  errors:
    lines_longer_than_80_chars: ignore
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Demo entry points are fine without library-level docs.
    public_member_api_docs: false
